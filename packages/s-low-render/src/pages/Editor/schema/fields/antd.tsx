import { FieldNode } from "../types";

const data: FieldNode[] = [
  {
    type: "Button",
    module: "antd",
    props: {
      children: "Button",
      type: "ghost",
    },
  },
];

export default data;
