import { Field } from "../types";

const editField: Record<string, Field[]> = {
  Button: [
    {
      key: "children",
      name: "文字",
      type: "Input",
    },
  ],
};

export default editField;
