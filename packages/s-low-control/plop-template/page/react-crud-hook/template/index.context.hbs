import React, { useContext } from 'react';

export const defaultValue = {
  filterForm: {}, // 过滤器表单
  modalVisible: false, // 新增编辑弹窗
  modalType: '',
  selectedRowKeys: [],
  modalData: {},
  // 表格列表
  dataSource: {
    success: false,
    error: false,
    done: true,
    data: [],
  },
  // 分页
  pagination: {
    current: 1,
    pageSize: 10,
    total: 0,
  },
};

const IndexContext = React.createContext(defaultValue);

function useIndexContext(): any {
  const value = useContext(IndexContext);

  return value;
}

export { IndexContext, useIndexContext };
