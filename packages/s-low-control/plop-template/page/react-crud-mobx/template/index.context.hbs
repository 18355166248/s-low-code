import { createContext, useContext } from 'react';

const initState: any = {};

export const {{firstUpperName}}Context = createContext(initState);

export const use{{firstUpperName}}Context = () =>
  useContext({{firstUpperName}}Context);
