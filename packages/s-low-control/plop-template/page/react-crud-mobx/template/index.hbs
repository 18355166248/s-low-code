import React, { FC } from 'react';
import FilterForm from './FilterForm{{firstUpperName}}';
import ModalDialog from './ModalDialog{{firstUpperName}}';
import { {{firstUpperName}}Context } from './{{name}}.context';
import { store } from './{{name}}.models';
import TableView from './Table{{firstUpperName}}';

// {{ZHName}}
const {{firstUpperName}}: FC = () => {
  return (
    <{{firstUpperName}}Context.Provider value={store}>
      <div className="p-6 bg-white rounded-lg shadow-outer">
        {/* 过滤器 */}
        <FilterForm />
        {/* 表格 */}
        <TableView />
        {/* 新增弹窗 */}
        <ModalDialog />
      </div>
    </{{firstUpperName}}Context.Provider>
  );
};

export default {{firstUpperName}};
